if $(git diff --cached --name-only --diff-filter=ACMR | sed 's| |\\ |g' | grep -E '^.+\/.+\.(tsx|ts|js)$')
then 
npx ts-standard $(git diff --cached --name-only --diff-filter=ACMR | sed 's| |\\ |g' | grep -E '^.+\/.+\.(tsx|ts|js)$') --fix
git update-index --again
fi